# VisTest

**Визуальное регрессионное тестирование, которое разворачивается внутри вашего
контура.** Работает без облака, заводится без написания кода и объясняет,
*почему* проверка упала, а не показывает проценты.

*Read in English: [README.md](README.md)*

---

## Что это

| | |
|---|---|
| **Данные не покидают контур** | Нет облака, нет телеметрии, нет обязательного исходящего трафика. Ставится на машину без интернета. |
| **Ноль кода на входе** | Рекордер мышью, `--suite pages.yaml`, pytest-фикстура или `POST /api/check` из чужого стека — четыре входа. |
| **Понятно, почему упало** | Не «не совпало на 3.2%», а `button[data-testid=submit] сдвинулась на 14px вниз, severity 82, порог 50`. Движок открыт и объясним. |

Движок сравнения совмещает **ΔE00** (перцептивное цветовое расстояние),
**SSIM** (структурное сходство), субпиксельное **выравнивание** и
автоматическое подавление шума. Пиксель считается изменившимся, только если
на это указывают И цвет, И структура.

---

## Зоны игнорирования: по элементу, а не по прямоугольнику

Часы, карусель, случайный аватар, счётчик онлайна — то, что всегда разное и
сравнивать что бессмысленно. Зона игнорирования была четырьмя числами, и
работало это ровно до первого переноса блока. А переносится он как раз во время
редизайна, то есть когда масок больше всего.

Ломалось при этом дважды, и оба раза тихо. Прямоугольник оставался на прежнем
месте: туда переезжал другой элемент, и его изменения переставали проверяться —
маска, поставленная на аватарку, глушила цену. А блок, ради которого маску и
ставили, уезжал из-под неё и красил прогон каждый день.

Теперь зона привязывается к **элементу**. Обведите область мышью на странице
снимка — VisTest посмотрит, что там в DOM, и предложит зацепиться за элемент, за
его родителя или остаться на координатах. Устойчивость зацепки видна до выбора:
`data-testid` переживёт перестройку вёрстки, путь из шести тегов — нет.

Координаты при этом никуда не деваются, и в этом весь смысл конструкции:

* элемент нашёлся — маска встаёт туда, где он сейчас, в обоих кадрах сразу;
* DOM нет (старое сравнение, чужой набор PNG) — работают координаты, как раньше;
* элемент пропал — работают координаты, и об этом **говорится вслух**: заметка
  в разборе, пунктирная рамка и счётчик в редакторе зон. Маска, тихо
  переставшая работать, не отличима от работающей — ни на картинке, ни по
  вердикту, — поэтому она сообщает о себе сама.

Зоны старого формата продолжают работать без единого действия: зона без
селектора — это и есть старая зона, и путь для неё прежний.

Из разбора падения работает так же: кнопка «Ignore area» на карточке региона
уносит селектор, который движок уже нашёл, а не голый прямоугольник.

---

## Токены CI: доступ для пайплайна, а не для человека

Токен приёма прогонов был один на всю инсталляцию — переменная окружения
`VISTEST_INGEST_TOKEN`. Пока писал один пайплайн, этого хватало. Дальше
арифметика перестаёт сходиться: подрядчику надо выдать доступ, а есть только
общий секрет; человек ушёл из команды — менять надо во всех пайплайнах разом и
одномоментно, иначе половина сборок краснеет. И по журналу нельзя ответить, чей
пайплайн залил прогон: там у всех написано «ci-token».

Токен теперь принадлежит **проекту** и живёт своей жизнью — «Settings → CI
tokens»:

* имя, роль и срок; прогон чужого проекта им не залить — 403;
* в журнале аудита он подписывается своим именем и префиксом, не показывая
  секрета;
* **отметка последнего использования** — единственный ответ на вопрос «старый
  уже можно гасить?». Без неё ротация превращается в гадание;
* ротация выпускает сменщика с теми же правами и **не гасит старый**: пока
  пайплайны перекатываются, он обязан работать.

Токен показывается **один раз, при выпуске**. В базе лежит только его хеш —
секрет, который сервис умеет показать, утечёт вместе с доступом к сервису.

`VISTEST_INGEST_TOKEN` продолжает работать точно как раньше и одновременно с
проектными токенами: переходить можно по одному пайплайну.

---

## Три вида запуска тестов

Кнопка «Run» была одна и означала «в том браузере, который выберет код». Для
набора, который проверяет вёрстку, это половина ответа: страница ломается в
firefox и не ломается в chromium ровно так же часто, как наоборот.

Видов запуска теперь три, и это три разных вопроса — на вкладке «Tests» рядом с
«▷ Run», на карточке проекта рядом с каждой кнопкой прогона:

| Вид | Что получается | Когда |
|---|---|---|
| **Как решат тесты** | один прогон, браузер выбирает их код | как было; разбор одной страницы |
| **Один браузер** | один прогон в названном движке | «падает ли это в firefox» |
| **По одному на браузер** | **N прогонов**, у каждого свой вердикт и свой статус-чек в CI | идут **параллельно** — наборы эталонов у браузеров разные, писать друг другу они не могут |
| **Все браузеры · один прогон** | **один прогон** и один ответ «эта страница сломалась?» | браузеры внутри идут подряд |

Работает и для наших собственных тестов, и для подключённых проектов.

**Как браузер доходит до чужого кода.** Своего браузера в этом пути у VisTest
нет — его поднимает их conftest. Значит единственный способ — попросить их же
код, и способов ровно два: переменная `VISTEST_BROWSER` (выставляется всегда) и
флаг `--browser` (его вводит pytest-playwright; без плагина pytest падает на
«unrecognized arguments», поэтому наличие проверяется их же интерпретатором, а
не предполагается). Управляется полем `browser_option` в описании проекта:
`auto` (по умолчанию), `flag`, `env`, `none`.

**Отказ вместо вранья.** Проект, который запускается своей командой
(`npx playwright test`, `mvn`, `dotnet test`), браузер назвать не даёт: куда
его там вписать, знают только они. Такой прогон «во всех браузерах» трижды
запустил бы один и тот же — и показал бы зелёное по трём наборам эталонов, то
есть покрытие, которого нет. Поэтому VisTest отказывается и называет причину, а
в меню пункты видны отключёнными с этой же причиной в подсказке.

---

## Матрица браузеров и разрешений

Один снимок — несколько вариантов. Раньше «эта страница на chromium/firefox при
1440, 768 и 390» означало шесть запусков и шесть несвязанных наборов эталонов, а
на один и тот же сдвиг подвала приходилось отвечать шесть раз. Это не строгость,
это способ приучить человека жать «принять» не глядя.

```yaml
# vistest.yaml
matrix:
  browsers: [chromium, firefox]
  viewports: ["1440x900", "768x1024", "390x844"]
  base_viewport: "1440x900"   # его эталоны остаются ровно там, где лежат
```

```bash
python run.py matrix        # во что это раскроется — до запуска
```

Шесть вариантов, один прогон, один вопрос. У каждого варианта свой набор
эталонов: кадр firefox при 390 не с чем сравнивать в наборе chromium при 1440.
Браузер был частью ключа всегда, размер добавляется суффиксом —
`linux-chromium-1x-390x844`.

**`base_viewport` — единственное, что стоит задать явно.** Его эталоны остаются
под прежним ключом, без суффикса, то есть там, где лежат все уже снятые:
включение матрицы не обесценивает ни одного из них. Не задать — базовым станет
первый в списке, и тогда перестановка двух строк переносит набор эталонов на
диске, а выглядит как правка форматирования.

Размер варианта перекрывает размер из паспорта снимка — матрица это заявление
про весь набор, а паспорт про один снимок. Снимки, у которых свой размер не
совпал с базовым, называются в логе прогона вслух: это единственное место, где
матрица меняет смысл уже снятого эталона, и промолчать здесь значило бы выдать
пачку падений без объяснения.

Из интерфейса: на вкладке «Baselines» появляется кнопка «Run the matrix» — она
показывает, во что раскрывается матрица и сколько это проверок, до запуска. На
странице прогона варианты одного снимка стоят под общим заголовком, а в разборе
между ними можно переключаться, не возвращаясь в список.

---

## Требования

- Python 3.10+
- Для съёмки в браузере: Playwright 1.47 и его сборка Chromium (ставятся командой `setup`)
- Опционально: Docker — для одинаковых эталонов на разных машинах

Самому ядру сравнения нужны только `numpy`, `opencv-python-headless` и
`pillow`. Всё остальное — опциональные зависимости.

---

## Быстрый старт

```bash
git clone <адрес-репозитория> && cd visual-testing

# Windows
.\run.ps1 setup          # venv + зависимости + браузеры
.\run.ps1 ui             # ревью-UI на http://127.0.0.1:8420/ui/

# Linux / macOS
./run.sh setup && ./run.sh ui
```

`run.py` сам определяет ОС, создаёт venv в нужном месте (`Scripts` vs `bin`),
ставит браузеры Playwright и открывает интерфейс. Команда
`python run.py doctor` покажет, чего не хватает.

### Сначала — правда о вашем проекте

```bash
python run.py doctor https://your-app.example.com
```

Страница грузится несколько раз подряд, ничего между загрузками не меняется —
значит любое расхождение здесь ложное по построению. Отчёт показывает, сколько
ложных падений вы получили бы **без** подавления и **с** подавлением VisTest,
и что именно у вас шумит: часы, анимации, ленивая загрузка, шрифты.

Это стоит запустить до того, как поверить инструменту: если шумит сам проект,
об этом лучше узнать в первые десять минут.

---

## Эталоны без написания кода

```bash
python run.py record https://your-app.example.com
```

Открывается браузер с панелью. Нажимаете «Снять страницу» — получаете эталон
**и готовый файл pytest + Playwright**, который можно принять на ревью и
править руками. Не запись во внутреннем формате, а обычный тест.

То же самое кнопкой: **«Эталоны» → «● Записать эталоны мышью»**.

Page objects вместо селекторов прямо в тесте:

```bash
python run.py record https://your-app.example.com --pom
```

Пересборка тестов из паспортов эталонов (ручные правки сохраняются):

```bash
python run.py codegen --pom
```

Снять эталоны по URL, вообще не открывая браузер руками:

```bash
python run.py snap https://your-app.example.com/checkout
```

---

## Использование в тестах

```python
def test_checkout(page, visual):
    page.goto("https://shop.example/checkout")
    visual.assert_screenshot("checkout.png")
```

Первый прогон создаёт эталон, последующие сравнивают. Никаких
`mask_selectors` писать не нужно: динамика определяется автоматически.

Точечная настройка, когда нужна:

```python
visual.assert_screenshot(
    "checkout.png",
    clip_selector="#order-summary",    # только компонент — быстрее и стабильнее
    fail_severity=40,                  # мягче порог для этой страницы
    mask_selectors=["#promo-banner"],  # ручная маска, если авто-детекта мало
)
```

Сравнить два PNG без браузера:

```bash
vistest compare expected.png actual.png -o diff/
```

---

## Подключение уже написанных тестов

VisTest умеет запускать существующий набор тестов, не меняя в нём ни строки.
Набор описывается один раз:

```bash
python run.py project add /путь/к/вашим-тестам
python run.py project list
python run.py project run your-tests
```

Подключённый набор запускается **окружением VisTest** — pytest, Playwright и
браузеры там уже есть, — а ваш код импортируется как есть: корень репозитория
добавляется в `PYTHONPATH`.

### Наборы не на Python

Набор, который запускается своей командой, подключается так же: указываются
`runner` и команда, а вердикт VisTest выносит по картинкам, оставшимся после
прогона.

```yaml
projects:
  web-e2e:
    runner: command
    command: [npx, playwright, test]
    root: /srv/web-e2e
    suite: auto            # или playwright | cypress | jest-image-snapshot | …
```

`suite` — это **профиль**, единственное место, которое знает, как чужой
инструмент выглядит на диске. Он отвечает ровно на три вопроса: чем набор
запускается, в какие каталоги кладёт картинки и как их называет. Playwright
оставляет в `test-results` тройку `login-actual.png`, `login-expected.png`,
`login-diff.png`; Cypress пишет `login.actual.png`; jest-image-snapshot кладёт
`login-received.png` рядом с `login-snap.png`; BackstopJS держит два
параллельных каталога. Каждая из этих раскладок — один декларативный профиль.

Из коробки опознаются **Playwright**, **Cypress**, **jest-image-snapshot**,
**BackstopJS**, **WebdriverIO**, **pytest**, а также формы запуска для
**Maven**, **Gradle** и **dotnet test**. `auto` выбирает профиль по маркерам в
вашем репозитории: `playwright.config.ts`, `cypress.config.js`,
`backstop.json`, `pom.xml`, `conftest.py`. Если не опознали — берутся самые
широкие правила, а не догадка.

Если раскладка не совпала ни с одним профилем — а на JVM это нормальный
случай, общепринятого соглашения там нет, — её описывают, а не форкают код.
Одно регулярное выражение на вид, имя снимка — в группе `name`:

```yaml
    search_dirs: [target/screenshots]
    naming:
      actual:   'snap_(?P<name>.+)_new\.png'
      expected: 'snap_(?P<name>.+)_base\.png'
      diff:     'snap_(?P<name>.+)_diff\.png'
```

Два ограничения, о которых честнее знать до подключения, — ни одно из них не
чинится с нашей стороны. Перехват (подмена сравнения внутри их теста, с
DOM-атрибуцией) — это плагин pytest, и в процессе Node или JVM его не будет
никогда: чужой набор всегда разбирается по готовым картинкам. А Playwright
пишет эти картинки **только при неудачном сравнении**, то есть зелёный прогон
Playwright не оставляет ничего для разбора.

Следующий инструмент добавляется одним `SuiteProfile` в
`vistest/suites/builtin.py` и строкой в `PROFILES`. Ни раннер, ни сборщик
результатов о нём знать не обязаны.

Секреты в описание проекта не попадают. Значения подставляются по имени
(`${YOUR_APP_PASSWORD}`) из окружения или из `.vistest/secrets.env`, см.
`.vistest/secrets.env.example`. Если переменная не задана, прогон падает с
понятной ошибкой — а не вводит пустой пароль.

### Три входа и как выбрать

|  | Чем платите | Что получаете |
|---|---|---|
| **Мы запускаем ваш набор** — `runner: command` | Ваш инструмент должен быть в образе VisTest: для Node это `docker/Dockerfile.node` и проект, смонтированный вместе с `node_modules` | Одна кнопка, полный вердикт, в вашем CI ничего не меняется |
| **Вы запускаете, мы читаем результат** — `vistest project ingest` | Ничем. Вашего рантайма в нашем образе не нужно вовсе | Тот же вердикт, и единственный разумный вариант для JVM и .NET |
| **Ваш тест зовёт нас** — `POST /api/check` | Одна строка в тесте | Вердикт прямо в тесте, и прошедший снимок — тоже результат |

Для набора не на Python начинать стоит со второго:

```bash
# их CI, сразу после прогона набора
vistest project ingest web-e2e --dir test-results --browser chromium
```

Ничего не запускается: VisTest читает картинки, которые прогон уже оставил,
выносит по ним вердикт своим движком и заводит прогон в общей истории. То же
действие есть в интерфейсе — «⋯ → Read ready artifacts».

Третий вход — один вызов с любого языка:

```bash
curl -H "X-VisTest-Token: $VISTEST_TOKEN" \
     -F image=@shot.png -F name=checkout.png -F project=web-e2e \
     http://127.0.0.1:8420/api/check
```

Клиент для Node (`clients/`, пакет `vistest-client`) снимает страницу вашим же
драйвером, стабилизирует её тем же скриптом, что и питоновский раннер, и
закрывает прогон одним вызовом:

```js
const vt = new VisTest({ apiUrl, project: 'web-e2e', runKey: process.env.CI_JOB_ID });
await vt.checkPage(page, 'checkout.png');
await vt.finish();          // одна запись в истории вместо россыпи проверок
```

Для набора на Playwright, который никто не станет править, есть репортёр — ноль
строк в тестах:

```ts
reporter: [['list'], ['vistest-client/playwright-reporter',
                      { apiUrl, project: 'web-e2e' }]]
```

Он забирает тройку `expected / actual / diff` из их же отчёта, в первый раз
берёт эталон из ИХ `expected`, а факт отправляет на сравнение. Ограничение
стоит знать до подключения: Playwright прикладывает эти картинки **только при
падении**, поэтому зелёный прогон не оставляет ничего для разбора.

У Cypress такого ограничения нет: `after:screenshot` — точка, предусмотренная
им самим, и срабатывает она на каждый снимок, включая прошедшие:

```js
setupNodeEvents(on, config) { vistest(on, config, { project: 'web-e2e' }); }
```

Для JVM есть `clients/java/VisTest.java` — один класс на `java.net.http`,
Java 11+, без зависимости в чужом `pom.xml`. Запускается и прямо из оболочки:
`java VisTest.java <url> <project> <name> <file.png>`. Java-агента нет
намеренно: агент, подменяющий сравнение внутри чужого набора, — самое
непроверяемое, что есть в планах, а для JVM первым выбором всё равно остаётся
`vistest project ingest`.

**Доступ.** На одиночной установке всё перечисленное работает как есть. Общей
нужен токен — на проект из «CI-токенов» или общий `VISTEST_INGEST_TOKEN` — в
заголовке `X-VisTest-Token`; клиенты берут его из `VISTEST_TOKEN`. «Не
настроил» не должно означать «открыто».

---

## Сервис

```bash
python run.py ui                    # локально
python -m vistest.cli serve         # без открытия браузера
```

- **Прогоны / Разбор** — просмотр диффов, приём и отклонение эталонов.
- **Эталоны** — версионируются, с историей и откатом.
- **Тесты** — открыть, поправить, сохранить и прогнать записанные тесты из UI.
- **Метрики** — false-fail rate, время до разбора, накопленная статистика.
- **Диагностика** — отчёт `doctor` и проверка окружения.
- Командная палитра: `⌘K` / `Ctrl+K`.

### Роли и доступ

Три роли: `viewer` (смотреть), `reviewer` (принимать/отклонять, запускать
прогоны), `admin` (пользователи, секреты, подключение проектов, запись мышью).

Пока не заведён первый администратор, вход открыт и всё работает как в
одиночном режиме; как только админ появился — аутентификация включается сама:

```bash
python -m vistest.cli user add anna --role admin
```

Остальных удобнее приглашать из **«Настройки → Команда»** одноразовой ссылкой:
человек сам задаёт логин и пароль, пароль не ходит по чатам. Приём прогонов
из CI остаётся открытым — у раннера сессии нет.

### Команда на ВМ

Стандартный `docker/docker-compose.yml` — лёгкий образ без браузера, запись,
проекты и секреты по умолчанию отвечают только на localhost. Для общей
установки есть профиль с браузером и окном записи (noVNC):

```bash
docker compose -f docker-compose.team.yml up -d --build
docker compose -f docker-compose.team.yml exec vistest \
  python -m vistest.cli user add anna --role admin
```

Порты `8420` (UI) и `6080` (окно записи) должны быть доступны команде.
**Администратора заводите сразу** — без него разблокированные операции открыты
всем, кто дотянется до порта.

Если базовый образ не тянется, укажите внутреннее зеркало, не трогая файлы:

```bash
VISTEST_BASE_IMAGE=<зеркало>/playwright/python:v1.47.0-jammy \
  docker compose -f docker-compose.team.yml build
```

---

## Настройка

Всё в `vistest.yaml`. Три пресета:

| Пресет | ΔE00 | fail_severity | Когда |
|---|---|---|---|
| `strict` | 1.2 | 10 | дизайн-система, компоненты в изоляции |
| `balanced` | 2.3 | 25 | по умолчанию |
| `loose` | 4.0 | 45 | «живые» страницы с контентом от CMS |

Локальные переопределения — в `vistest.local.yaml` (в .gitignore). Пороги
глобально и на проект настраиваются также через API и хранятся в базе, где
имеют приоритет над файлом.

### Эталоны на разных машинах

Рендер текста в Windows, macOS и Linux физически разный, поэтому эталоны
разделены по платформам (`baselines/win-chromium-1x/`,
`baselines/docker-chromium-1x/`). Локально вы сравниваетесь со своим набором,
CI — с докерным. Единый эталон для всех даёт только контейнер с
зафиксированными версией браузера и набором шрифтов:

```bash
python run.py docker         # поднять сервис
python run.py docker test    # прогнать тесты в контейнере
```

---

## Проверка движка

```bash
pytest tests/                           # инварианты: шум не валит, регрессы находятся
pytest tests/api                        # автотесты API: вход, роли, приглашения, команда
python tests/benchmark.py --artifacts   # таблица «шум / сигнал» + картинки
python tests/benchmark.py --compare     # VisTest против absdiff, pixelmatch, Playwright
```

Бенчмарк на синтетике — основной инструмент подстройки порогов: после любой
правки конфигурации сразу видно, не начал ли движок ловить шум. Настоящие
скриншоты для этого не годятся — в них неизвестно, что «правда».

Главная метрика — **false-fail rate**: доля неизменённых по существу страниц,
на которых инструмент дал красный тест.

Статистика с реальных прогонов набирается сама:

```bash
vistest evidence                        # цифра за 90 дней
vistest evidence --format md -o EVIDENCE.md
```

Ложным считается падение, которое человек посмотрел и принял как новый эталон.
Отчёт всегда считает доверительный интервал и долю неразмеченного — без них
цифра была бы враньём.

Соответствие ΔE00 контрольным парам из CIE Technical Report (Sharma et al.,
2005) проверяется отдельно.

---

## Список команд

```
python run.py setup                 поставить зависимости и браузеры
python run.py ui                    поднять сервис и открыть интерфейс
python run.py doctor [URL]          проверить окружение либо шум проекта
python run.py record URL            записать эталоны мышью
python run.py snap URL              снять эталоны по URL
python run.py check IMAGE           проверить готовый PNG против эталона
python run.py compare A.png B.png   сравнить два PNG без браузера
python run.py test [аргументы]      прогнать тесты
python run.py update                перезаписать эталоны
python run.py list | rm | prune     эталоны и удаление старых прогонов
python run.py codegen [--pom]       пересобрать тесты из паспортов эталонов
python run.py matrix                во что раскрывается матрица браузеров и разрешений
python run.py project add|list|run|rm    подключённые наборы тестов
python run.py report RUN            офлайн HTML-отчёт по прогону
python run.py evidence              накопленная статистика качества
python run.py push                  выгрузить прогоны, сделанные офлайн
python run.py docker [up|test|down] запуск в контейнере
```

`python -m vistest.cli` даёт те же команды плюс `serve` и `user`.

### Вход через корпоративный каталог

LDAP и Active Directory, настраивается в **Settings → Corporate directory**.

```bash
pip install "vistest[ldap]"          # ldap3, чистый Python, ставится офлайн
VISTEST_LDAP_BIND_PASSWORD=…         # пароль сервисного аккаунта
```

Дальше в интерфейсе: сервер (`ldaps://dc.company.local`), базовый DN, DN
сервисного аккаунта и соответствие групп ролям, по строке на группу:

```
CN=QA Leads,OU=Groups,DC=company,DC=local=admin
CN=QA,OU=Groups,DC=company,DC=local=reviewer
```

Кнопка **Test the connection** отвечает по шагам — подключились, сервисный
аккаунт вошёл, человека нашли, роль была бы такая, — потому что «не работает»
чинить нечем.

Три вещи, которые стоит знать до включения:

- **Локальные учётные записи продолжают работать.** Каталог спрашивается
  вторым. Если он недоступен, администратор с локальным паролем всё равно
  войдёт и выключит интеграцию.
- **Человек появляется при первом входе.** Ни импорта, ни задания
  синхронизации. Роль пересчитывается из групп на каждом входе, поэтому выход
  из группы действует сразу — кроме роли, поднятой вручную в VisTest: она
  сохраняется.
- **Учётная запись из каталога не открывается локальным паролем**, поэтому
  отключение человека в каталоге действительно означает, что он не войдёт.

### Перенос эталонов между инсталляциями

Не то же самое, что резервная копия: часть набора, вливаемая в инсталляцию, у
которой уже есть свои эталоны. Для dev → stage, для передачи работы от
подрядчика, для команды, которая разделилась надвое.

```bash
# забрать набор одного проекта
python -m vistest.cli baselines export shop.tar.gz --project shop

# посмотреть, что сделает импорт, и ничего не менять
python -m vistest.cli baselines import shop.tar.gz --mode update --dry-run

# влить
python -m vistest.cli baselines import shop.tar.gz --mode update
```

Три режима, потому что у вопроса «а этот снимок здесь уже есть» три разумных
ответа: `new` оставляет своё и берёт только недостающее (по умолчанию);
`update` кладёт присланную картинку **новой версией**, так что предыдущая
остаётся в истории и к ней можно откатиться из интерфейса; `replace` отдаёт
победу присланному набору вместе с историей.

То же самое доступно по API (`GET /api/baselines/export`,
`POST /api/baselines/import`) для роли reviewer.

История утверждений не переносится. Подпись под «я это посмотрел, так
правильно» принадлежит тому, кто её поставил, и той инсталляции, где он это
сделал.

### Резервные копии

```bash
# Эталоны, база и список подключённых проектов — одним архивом.
python -m vistest.cli backup vistest-2026-09-05.tar.gz

# Что внутри, не распаковывая
python -m vistest.cli restore vistest-2026-09-05.tar.gz --show

# В пустой каталог данных; --force — поверх живой инсталляции
python -m vistest.cli restore vistest-2026-09-05.tar.gz
```

База снимается через `VACUUM INTO`, поэтому копию можно делать на работающем
сервисе — а вот копирование `vistest.db` руками в режиме WAL даёт файл,
который откроется. Иногда.

`secrets.env` в копию **не** входит, пока не попросить `--with-secrets`: копия
уезжает в файловое хранилище и в тикеты, и пароли от стендов не должны ездить
с ней молча. Артефакты прогонов тоже не входят — их пересоздаёт следующий
прогон. Смысл копии в эталонах, а их не пересоздаёт ничто: это накопленные
решения людей.

---

## Структура

```
vistest/core/          движок сравнения (numpy/cv2, без браузера и сети)
vistest/service.py     единая точка проверки для pytest / HTTP / CLI / записи
vistest/capture/       стабилизация, N-shot, DOM-снепшот
vistest/integrations/  универсальный драйвер (Playwright/Selenium) и адаптеры
vistest/record/        режим записи мышью, snap по URL, генерация кода
vistest/ai/            attribution / обучаемый гейт / перцептивный фильтр
vistest/render/        визуализация различий
vistest/api/           FastAPI + SQLite + метрики + /api/check + люди и команда
vistest/storage/       версионируемые эталоны
frontend/              ревью-UI, без сборки
control/               операторская панель для инстансов на команду
scripts/               провижининг и вспомогательное для бенчмарка
deploy/                шаблоны compose и Caddyfile
clients/               клиент для Node и контракт API для других языков
docker/                образы: Dockerfile.api, .full, .runner
tests/                 синтетический бенчмарк движка и автотесты API
examples/              демо-страница и примеры тестов
```

Все входы — pytest-фикстура, HTTP-эндпоинт, CLI и режим записи — ходят в один
`CheckService`. Пороги, маски, артефакты и метрики одинаковы независимо от
того, откуда пришёл снимок.

---

## Безопасность

- Секреты читаются из окружения или `.vistest/secrets.env`, который в
  .gitignore. Реальные пароли коммитить нельзя; ожидаемые имена — в
  `.vistest/secrets.env.example`.
- Сохранённые сессии браузера (`.vistest/state/`) — это действующий доступ к
  вашему стенду. Они в .gitignore, так и оставьте.
- **Эталоны и DOM-снепшоты содержат содержимое снятых страниц.** Если вы
  тестируете внутреннюю систему, относитесь к `.vistest/baselines/` как к
  данным той же чувствительности и не выкладывайте их в публичный репозиторий.
- Редактор переменных в UI по умолчанию доступен только с localhost
  (`VISTEST_SECRETS_UI=local`); `off` выключает его, `all` открывает осознанно.

### За обратным прокси

VisTest по умолчанию **не верит** заголовкам `X-Forwarded-*` ни от кого, и это
не осторожность ради осторожности — это разница между границей и её видимостью.

Часть действий разрешена «только с машины сервиса»: подключение проекта (а это
запуск произвольного процесса), правка и запуск тестов, редактор переменных,
запись с мышью. Проверка смотрит на адрес соединения. Если процесс запущен с
`--forwarded-allow-ips *`, uvicorn берёт этот адрес из **первого** элемента
`X-Forwarded-For` — то есть из значения, которое пишет сам вызывающий: прокси
свой адрес *дописывает* в конец, а не заменяет строку. Один заголовок в запросе
открывает после этого всё перечисленное.

Поэтому: запускайте без `--proxy-headers` (в наших образах уже так) и назовите
свой прокси явно.

```bash
# Адрес или подсеть вашего nginx/Caddy/Traefik. Имя тоже подходит — в
# docker-compose это обычно имя сервиса.
VISTEST_TRUSTED_PROXIES=10.0.0.5,172.18.0.0/16

# TLS терминируется снаружи, а зависеть от заголовков не хочется вовсе:
VISTEST_COOKIE_SECURE=1
```

Переменная не задана — не доверяем никому: адрес соединения берётся как есть, а
флаг `Secure` на куке ставится только по настоящему HTTPS или по
`VISTEST_COOKIE_SECURE=1`. Задана — `X-Forwarded-Proto` от этого прокси решает
судьбу флага, а адрес клиента в журнале и в счётчике неудачных входов берётся
из цепочки. Но «локально» по-прежнему означает само соединение, а не заголовок.

### Схема API

`/docs`, `/redoc` и `/openapi.json` закрыты. Это перечень всех роутов, тел
запросов и имён полей — то есть пособие для того, кто изучает сервис снаружи.
Включаются на время, когда нужны:

```bash
VISTEST_DOCS=on
```

### Пределы приёма

Вход для чужих наборов (`POST /api/check`) принимает снимок от любого CI,
поэтому у него есть потолки — все переопределяются:

| Переменная | По умолчанию | Что ограничивает |
|---|---|---|
| `VISTEST_MAX_ARTIFACT_MB` | 40 | одну присланную картинку или артефакт |
| `VISTEST_MAX_FRAMES` | 8 | дополнительные кадры на одну проверку |
| `VISTEST_MAX_PIXELS` | 60000000 | пиксели после декодирования — маленький PNG разворачивается в гигабайты |
| `VISTEST_MAX_BODY_MB` | 64 | любое другое тело запроса (у загрузок свои, более строгие пределы) |
| `VISTEST_ENGINE_MAX_PIXELS` | 80000000 | сколько движок соглашается держать в памяти |

### Адрес API

Роуты доступны и как `/api/...`, и как `/api/v1/...`. Клиентам на других языках
стоит использовать вариант с версией: когда контракт однажды изменится,
`/api/v2` станет отдельной вещью, а `/api/v1` продолжит работать.

Архив проекта (`POST /api/projects/upload`) — 200 МБ, 2 ГБ в распакованном
виде и 50 000 записей.

---

## Лицензирование

Словом «лицензия» здесь называются две разные вещи, и путать их дорого,
поэтому они разведены намеренно:

- **Лицензия на исходный код — AGPL-3.0-or-later.** Она про код: читайте,
  меняйте, запускайте; если запускаете изменённую версию как сетевой сервис —
  публикуйте изменения. Ничто ниже этого не отменяет.
- **Коммерческий ключ** — то, что получает заказчик. В нём написано, кому он
  выдан, до какого числа и на сколько проектов и пользователей.

### Редакции

| | Бесплатный режим | С ключом |
|---|---|---|
| Движок, интерфейс, интеграция с CI, клиенты | всё | всё |
| Подключённых проектов | 2 | сколько куплено |
| Активных пользователей | 5 | сколько куплено |
| Поддержка и обновления | сообщество | по договору |

Бесплатный режим — не урезанное демо: движок, разбор и все интеграции те же
самые. Вся разница в лимитах.

### Установка ключа

Три места, проверяются в этом порядке — переменная сильнее, чтобы контейнер
настраивался так, как настраивают контейнеры:

```bash
VISTEST_LICENSE="eyJlZGl0aW9uIjoicHJvIiw…"   # переменная окружения
.vistest/license.key                          # том данных
```

…либо вставьте ключ в **Settings → Licence** в интерфейсе: он проверяется на
месте и сохраняется на том данных.

Проверка полностью оффлайновая: в ключе подпись RSA, открытая половина зашита
в VisTest, и для проверки не нужны ни сеть, ни дополнительные пакеты.
Инсталляции в закрытом контуре никуда ходить не приходится.

### Что происходит, когда срок кончился

У заказчика ничего не забирают. Эталоны читаются, история читается, разбор
работает — это ваши данные и ваши решения.

Тридцать дней после даты окончания инсталляция работает как обычно и говорит
об этом на каждом экране. После этого — и только тогда — **новые прогоны не
принимаются**: ответ 402 с объяснением. Установка действующего ключа
возвращает всё сразу.

### Выпуск ключей (для владельца продукта)

```bash
python scripts/issue_license.py --new-key ~/.vistest-signing/private.pem
python scripts/issue_license.py --key ~/.vistest-signing/private.pem \
    --customer "ACME GmbH" --expires 2027-09-01 --projects 10 --users 25
```

Закрытая половина не попадает ни в репозиторий, ни в бэкап, уезжающий с вашей
машины, ни в тикет поддержки.

### Честно про принуждение

Исходники открыты, значит проверку можно удалить и пересобрать. Это свойство
AGPL, а не дыра, которую надо заткнуть, и никакая обфускация этого не изменит.
Ключ нужен для другого: чтобы условия были названы и проверяемы — чтобы честный
заказчик знал, что он купил, а продление было разговором, а не ревизией.

---

## Лицензия

AGPL-3.0-or-later. См. [LICENSE](LICENSE) и [NOTICE](NOTICE).

Если вы запускаете изменённую версию VisTest как сетевой сервис, AGPL обязывает
вас предоставить пользователям этого сервиса полный исходный код вашей
изменённой версии. Коммерческая лицензия без этого обязательства доступна —
контакты в [NOTICE](NOTICE).

«VisTest» и логотип VisTest — товарные знаки автора проекта, действие AGPL на
них не распространяется. Форки должны быть переименованы и переоформлены.
